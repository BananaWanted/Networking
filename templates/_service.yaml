apiVersion: v1
kind: Service
metadata:
  name: {{ template "Networking.fullname" . }}
  labels:
    app: {{ template "Networking.name" . }}
    chart: {{ template "Networking.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "Networking.name" . }}
    release: {{ .Release.Name }}
